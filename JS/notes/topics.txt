Javascript
  -- introduction
     how many ways js can be added?2(internal,external)
     is console part of js?
     along with console, browser contains some tools- google developer tools
     -- var,let,const
  -- data types
  -- primitve(number,string,boolean,undefined,null)
  -- js engine
  -- arrays(array fns - foreach,map,filter,reduce)
  -- functions(callback fn, function expression,arrow fn,closure)
  -- objects
  -- if,switch
  --for,for..in,for--of,forEach
  -- var,let,const
  -- DOM
  -- events(event propagation - bubbline,capturing,event delegation)
  -- ASYNC
    -- Timers (setTimeout,setInterval)
    -- callback hell 
    -- promises 
    -- fetch
    -- async await
  -- ES6
      -- Arrow functions
      -- var,let,const
      -- template literals
      -- rest,spread
      -- destructuring arrays,objects
      -- OOPs
  -- BOM(localstorage,sessionstorage)
  -- libraries
  -- main project
  -- interview
  -- doubt clearing